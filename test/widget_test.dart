import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App skeleton smoke test', (WidgetTester tester) async {
    // Placeholder — real tests will be added per feature.
    expect(1 + 1, equals(2));
  });
}
